# AGENTS.md

本仓库是中文书《前线部署工程师：人工智能时代的客户价值交付秘籍》（作者：范冰）的写作仓库。
纯 Markdown 内容项目，**没有构建系统、没有测试、没有依赖、没有任何配置文件**（无 package.json / pyproject.toml / Makefile 等）。所有工作都是写作与编辑，技术栈只有 Markdown + Git。

## 项目概况

- 本书研究「前线部署工程师」（Forward Deployed Engineer，FDE）这一岗位：定义、交付方法论、真实案例。
- 全文免费公开，主要面向 GitHub 在线阅读；另提供一份根目录合并版 PDF 供整本下载。
- 语言为简体中文，编辑时一律使用中文（代码、文件名等标识符除外）。

## 仓库结构

- `README.md` — 项目说明、作者介绍、目录（含各章节链接）、整本 PDF 下载链接。**目录顺序与文件命名约定为真**，改动章节文件时同步更新此处链接。
- `VERSION` — 单行纯文本，当前发布版本号（当前为 `1.0.11`）。
- `00-自序.md` — 自序；`NN-第N章-标题.md` — 正文章节（01–08），`NN` 为两位数前缀，文件名即目录标题。
- `09-后记-*.md`、`10-附录A-*.md`、`11-附录B-*.md`、`12-附录C-*.md` — 后记与三个附录。
- `index.html` — GitHub Pages 阅读主页（单文件静态页，内联 CSS/JS，无构建）。通过 CDN 引入 marked.js，直接 fetch 根目录章节 `.md` 渲染为站内阅读视图，hash 路由为 `#/read/NN`。**内嵌章节清单（`CHAPTERS` 数组）与 PDF 版本号**：章节文件改名/增删、发布新版本时须同步修改。
- `.nojekyll` — 关闭 GitHub Pages 的 Jekyll 处理，保证 `index.html` 与 `.md` 按静态文件原样服务。Pages 来源为 `main` 分支根目录。
- `前线部署工程师（FDE）v<VERSION>.pdf` — 根目录合并版 PDF。**文件名内嵌版本号**，发布新版本时连同 `VERSION`、README 下载链接、`index.html` 中的 PDF 文件名一起改。
- 章节文件均位于根目录（非子目录），便于 GitHub 在线浏览——这是有意为之，不要把它们挪进 `chapters/` 之类子目录。

## 写作与编辑约定（重要）

历史 commit 已确立一套中文写作规范，编辑现有内容时务必遵守：

- **消除翻译腔**：用自然中文表达，避免「被…所」「对于…而言」「它是一个…的」之类西式句式。
- **消除专业黑话**：普通人看不懂的术语必须改写或加注释；不可避免的专业词首次出现时用通俗话解释。
- **统一段落主语**：同一段内不要频繁切换主语，避免读起来跳跃。
- **中文标点**：全角中文标点；引用用「」（直角引号），书名用《》。
- **数字与单位**：阿拉伯数字与中文/单位之间留半角空格（如「95% 的项目」「2000 万美元」）。
- **书名统一为《前线部署工程师》**：全文不要混用其他译名（如「前向部署工程师」等）。
- 章节正文按 `## N.M 小节标题` 组织小节，章首可有题记引文（blockquote）。

## 资料出处规范

- 所有数据、案例、引用**必须**能在 `12-附录C-全书案例索引与资料出处.md` 中找到对应出处。
- 正文提及具体数字或案例时，必要时标注「（出处见附录 C）」。
- 编辑附录 C 时保持「第一部分 案例索引（按章节）+ 第二部分 资料出处（按章节）」的两段式结构，编号在章节内连续。
- 新增正文数据/案例时，必须同步在附录 C 补充索引与出处。

## 发布新版本的联动改动

发布新版本（如 `1.0.5` → `1.0.6`）需同步四处：

1. `VERSION` 文件改为新版本号。
2. 重命名 PDF 文件中的版本号。
3. `README.md` 中「整本下载」链接的文件名与新 PDF 一致。
4. `index.html` 中的 `PDF` 常量改为新 PDF 文件名。

## 平台兼容

- 主要面向 GitHub 在线阅读：链接用相对路径 `.md`，确保在 GitHub 网页可直接点开。
- 不要引入需要构建步骤才能渲染的内容（如未被 GitHub 原生支持的扩展语法）。

## 验证方式

没有自动化测试。编辑后的检查靠人工与简单命令：

- `grep` 检查译名/标点规范，例如确认没有混入「前向部署工程师」等其他译名、没有半角引号混入正文。
- 改动章节标题或文件名后，检查 `README.md` 目录链接与实际文件一一对应。
- 发布前确认 `VERSION`、PDF 文件名、README 下载链接三处版本号一致。
